import 'package:flutter/material.dart';

class WorkHrPage extends StatelessWidget {
  const WorkHrPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('General/Hr currently in construction Field'),
      ),
    );
  }
}
