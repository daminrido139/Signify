import 'package:flutter/material.dart';

class AssignmentPageTeacher extends StatelessWidget {
  const AssignmentPageTeacher({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Assignmentpost")),
    );
  }
}
