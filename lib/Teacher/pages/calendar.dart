import 'package:flutter/material.dart';

class CalendarTeacher extends StatelessWidget {
  const CalendarTeacher({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
        body: Center(
      child: Text('Calendar Teacher'),
    ));
  }
}
